# Direct dependencies for the serving image, pinned. Bump versions deliberately.
# The three blocks at the bottom were added later and carry their own dates and
# rationale; see there.
#
# What backs the word "pinned", and why this paragraph replaced a date. This
# file used to open by naming a known-good build verified by hand in Docker on
# 2026-06-08 (linux/py3.11 — the Cloud Run runtime). That was the file's whole
# warrant, and nothing re-derived it: every merged bump left the sentence
# untouched while making it less true, because a version named below was not
# part of the build that date pointed at. With a bot proposing pins every
# Monday (#40, #53) the gap only widened.
#
# Since #87 the assertion is standing rather than dated. ci.yml's api-image job
# builds Dockerfile and runs `python -c "import api.main"` inside the resulting
# image on every pull request, so the resolve in this file and the app's import
# graph are both checked against the artifact that actually deploys, at the
# versions in the diff. A bump that breaks either is red before it merges.
#
# Dated verification notes do still appear further down — the 2026-07-28 batch,
# and the google-cloud-storage bump that moved alone beside it. Each records
# what was checked when that one change merged, which is worth keeping and is
# not the same claim as this one. None of them is the file's standing warrant;
# that is this paragraph's job now.
#
# The floor, so it is not over-read: the image job installs and imports, it does
# not call. CI runs keyless by design, so no live external client is exercised.
# The per-pin notes below say where that gap bites — google-cloud-storage and
# langsmith each carry one.
#
# Six pins below moved on 2026-07-28, taking the first batch of Dependabot's
# weekly output (#40) as one diff rather than six sequential rebases: the
# langchain group (langchain, langchain-groq, langgraph, langsmith), cohere,
# and yt-dlp. Two of those close advisories — yt-dlp GHSA-6v4j-43gg-vj32
# (high) and langchain GHSA-gr75-jv2w-4656 (medium).
#
# The four #37 pins below did not move, and that is a checked result, not an
# omission: langchain 1.3.14, langgraph 1.2.9 and langchain-groq 1.1.3 declare
# langchain-core floors of >=1.4.9, >=1.4.7 and >=1.4.0, all under the pinned
# 1.5.1, and langsmith 0.10.10 needs only pydantic>=2, typing-extensions>=4.0.0
# and requests>=2.0.0. So this batch did not tighten the solve — the loud
# failure that pin block exists to produce had nothing to fire on.
#
# Verified by CI on linux/py3.11: a real `pip install -r requirements.txt`
# (no resolver conflict) plus the pytest suite. No Docker build was run — this
# batch merged hours before #87 added the image job — so at merge time these
# versions were resolver-checked and test-checked, not image-checked. They are
# image-checked now, but by a later run over this file rather than by their own
# PR, which is the weaker claim of the two. langsmith is the one to watch on
# the next deploy: 0.8.18 → 0.10.10 is a 0.x minor, which carries no
# compatibility promise, and agent/rag_chain.py:354 imports it inside shipped
# code.
#
# google-cloud-storage moved separately on 2026-07-28 (#64), 2.18.2 → 3.13.0,
# and alone rather than in the batch above — Dependabot opens majors ungrouped
# on purpose (.github/dependabot.yml), because a major is not a
# review-by-reading change. This one is the exception that proves the rule, and
# the reason is the size of the call site rather than the size of the version
# jump: the whole of this repo's GCS usage is three lines at api/catalog.py:50
# — `storage.Client()`, `.bucket(...).blob(...)`, `.download_as_text()` — read
# once per GET /api/catalog to pull gs://scienceq-data/metadata.json. Nothing
# else under api/, agent/ or pipeline/ imports google.cloud, uploads, or names
# a bucket.
#
# The four breaking changes 3.0 declares, and why each misses that surface:
#   - google.resumable_media.common.{InvalidResponse,DataCorruption} moved to
#     google.cloud.storage.exceptions. Nothing here imports resumable_media, so
#     there is no import to update; and get_catalog wraps the read in a bare
#     `except Exception` that degrades to the live-only list, so either class is
#     caught. That fallback is what makes this change inert rather than merely
#     unreferenced.
#   - Blob.upload_from_file now requires bytes mode and raises TypeError
#     otherwise. Download-only path.
#   - Retries are now on by default for object upload, metadata update and
#     delete, reversing the old off-unless-generation-specified rule. All three
#     are mutations; this path performs none.
#   - Upload/download checksums default to "auto" (crc32c, falling back to md5
#     when the fast C extension is absent) where 2.x used md5 for most
#     downloads and None for most uploads. This is the only one that touches
#     the read, and it adds validation rather than removing it. It also pulls
#     google-crc32c into both images as a transitive.
# The 3.0 Python floor is >=3.7 and both images are 3.11, so that one is
# recorded, not a constraint. 3.0 also absorbed google-resumable-media, which
# it still declares for backwards compatibility and may drop later; since
# nothing here imports it, that removal lands inert too.
#
# Verification, and its limits — the same shape as the paragraph above but
# weaker in one place and newly stronger in another. Weaker: CI installs this
# file for real on linux/py3.11, so the resolve is checked, and since #52 the
# api-image job also builds Dockerfile and imports the app, so 3.13.0 is now
# image-checked at import. What no CI job does is exercise the GCS client
# against a real bucket, so this pin is checked as far as installing and
# importing, not as far as calling.
# Stronger: until this bump the catalog GCS path had no test at all — the
# import at api/catalog.py:48 is lazy and no test reached it, so a green suite
# on a google-cloud-storage bump was measuring nothing. tests/test_catalog.py
# now asserts, against the installed package, the four attributes those three
# lines resolve against, so a future major that removes or renames one turns a
# green Dependabot PR red instead of surfacing on the next request. What it
# deliberately does not do is touch the network: CI runs
# keyless by design, so the live read is still only exercised by a deploy. The
# check for that is GET /api/catalog returning entries with source "corpus" —
# read off the serving revision, which is not necessarily the latest one.
#
# requirements-dev.txt inherits this file via `-r`, so the pipeline image takes
# 3.13.0 and google-crc32c for code pipeline/ never imports. Pre-existing shape,
# already stated there, and #52's to narrow — not a reason to split the pin.
langchain==1.3.14
langchain-groq==1.1.3
langchain-pinecone==0.2.13
langgraph==1.2.10
langsmith==0.10.12
pinecone==7.3.0
cohere==7.0.8
youtube-transcript-api==1.2.4
python-dotenv==1.2.2
groq==0.37.1
fastapi==0.140.12
uvicorn[standard]==0.51.0
yt-dlp==2026.7.4
google-cloud-storage==3.13.0
slowapi==0.1.10

# ── Imported by name, previously left to the resolver (#37) ──────────────────
# Every one of these is imported directly by api/ or agent/, so it is a direct
# dependency regardless of which parent package also happens to pull it in.
# Until now none was declared here, so their versions came from whatever the
# resolver picked at build time — two builds of the same commit could ship
# different versions of code the app imports, with no diff to review.
#
# Versions below are what a clean `--no-cache` build of Dockerfile — the image
# this file is the manifest for — resolved on 2026-07-28 (linux/amd64, py3.11,
# the Cloud Run runtime), read out with importlib.metadata. Re-derive against
# Dockerfile, not Dockerfile.pipeline: the pipeline image installs
# requirements-dev.txt, a superset whose extra pins tighten the solve. Both
# happen to agree on these four today, but they do not agree in general — the
# superset holds langchain-openai at 1.3.4 where this file alone floats it to
# 1.4.1. Never re-derive on a developer machine: environment markers differ by
# platform and a Windows resolve reports a different set.
#
# starlette is the reason this is not just tidiness. fastapi==0.140.12 declares
# `starlette>=0.46.0` with no upper bound, and the resolver has already walked
# that across a major version to 1.3.1. api/limiter.py and api/routes.py import
# starlette.requests.Request directly, so absent this pin the next major lands
# in the serving image on a rebuild with no manifest change.
#
# The trade this accepts: bumping fastapi now requires a deliberate matching
# starlette bump instead of letting it float. That is the point — do not remove
# this pin as redundant just because fastapi already depends on starlette.
# langchain-core carries the same trade in a tighter form: 1.5.1 sits exactly
# on the floor declared by langchain-openai 1.4.1 (`>=1.5.1`), and
# langchain-openai is itself transitive and unpinned, so one of its future
# releases can raise that floor and fail this build outright. That failure is
# loud and shows up in a diff, which is the trade working, not breaking.
#
# Scope, so the next reader is not misled: this pins what api/ and agent/
# import by name. The transitive tree still floats almost entirely (anyio,
# httpx, tenacity, numpy, ...) and there are no hashes, so builds are more
# reproducible than before, not reproducible. The one transitive held despite
# nothing importing it is limits, pinned at the bottom of this file under a
# two-clause criterion #41 settled; the block there states what makes it the
# exception and works through h11 under uvicorn, which meets the first clause
# and is still not pinned. Successor releases are watched
# now: .github/dependabot.yml (#40) opens a weekly PR when a newer release of
# a pin below exists, grouping fastapi+starlette and the langchain train so a
# coupled bump arrives as one reviewable diff. The floating transitive tree is
# covered from the other side: Dependabot alerts and security updates were
# enabled on the repo on 2026-07-28, and those read the full dependency graph
# rather than this file. That half is a repository setting, so no file here
# can guarantee it is still on. Bumps still merge by hand either way; the bot
# only surfaces them.
langchain-core==1.5.2
pydantic==2.13.4
starlette==1.3.1
typing-extensions==4.16.0

# ── No longer imported by shipped source, and pinned anyway (#42, #52) ───────
# Written when Dockerfile COPYed pipeline/ wholesale, which put
# pipeline/sponsorblock.py:17 and pipeline/enrich_metadata.py:27 in the serving
# image importing requests by name — the same criterion the four above were
# pinned on. It was pinned only in requirements-dev.txt, which the API image
# never installs, so it floated here (2.34.2) while the pipeline image shipped
# 2.32.5: two production images, two versions of the same package.
#
# #52 narrowed that COPY to the four modules the API path actually reaches, and
# neither importer is among them. The criterion above therefore no longer
# reaches requests, and on the placement rule alone this pin would move to
# requirements-dev.txt. It stays here deliberately, on the same footing as the
# limits block below: an unimported package pinned under a narrow written
# criterion rather than by the general rule. Do not "correct" it back by
# reading the placement rule literally — that rule is about which file an
# imported package goes in, and this is the other case.
#
# The criterion, in one clause: requests still ARRIVES in the serving image, as
# a transitive of cohere, langsmith, pinecone and google-cloud-storage. Only
# the first-party import went away, not the package. Moving the pin to
# requirements-dev.txt would leave it installed in the API image at whatever
# the resolver picks, while the pipeline image held 2.34.2 through the `-r` —
# #42's divergence rebuilt, differing only in that no first-party line imports
# it any more.
#
# The advisory floor is the second reason, and it is why "nothing imports it,
# so let it float" is the wrong read: CVE-2026-25645 affects < 2.33.0 and an
# unpinned resolve is free to land there. A package present in a production
# image is in that image's scan surface whether or not first-party code names
# it — the scanner reads the installed set, not the import graph.
#
# Declared here rather than in requirements-dev.txt because that file inherits
# this one via `-r`: the pipeline image — where sponsorblock and
# enrich_metadata do still ship, since Dockerfile.pipeline COPYs pipeline/
# wholesale — resolves the same pin without restating it, so the two images
# converge on one version by construction.
#
# Converged up on 2.34.2 rather than down on the 2.32.5 requirements-dev.txt
# carried, because CVE-2026-25645 (GHSA-gc5v-m9x4-r6x2, medium, CVSS 4.4)
# affects < 2.33.0 — pinning down would have put the advisory in both
# production images instead of neither. It is not reachable from this codebase:
# the flaw is predictable temp-file naming in requests.utils.extract_zipped_paths(),
# which only direct callers hit, and both call sites here are a plain
# requests.get with params and a timeout (sponsorblock.py:36,
# enrich_metadata.py:73). Pinning to a flagged version anyway would buy nothing
# and cost every future image scan.
#
# The API image had already floated to 2.34.2, so this moves only the batch job,
# across 2.33.0 → 2.34.2. Runtime behaviour changed in that span only in greedy
# no_proxy matching and duplicate leading slashes in URI paths; neither touches
# a fixed-URL GET with no proxy set. The remainder is inline typing replacing
# typeshed, a PEP 517 build, and dropping Python 3.9 (both images are 3.11),
# plus two dependency floors 2.33.0 raised: urllib3 >=1.21.1 → >=1.26 and
# certifi >=2017.4.17 → >=2023.5.7. Both images resolve well clear of those
# (urllib3 2.7.0, certifi 2026.7.22), so they are recorded here, not a
# constraint to work around.
requests==2.34.2

# ── Imported by nothing, and it is where rate limiting actually happens (#41) ─
# Nothing under api/, agent/ or pipeline/ imports limits, so the criterion the
# two blocks above rest on — a package shipped source imports by name is a
# direct dependency — does not reach it. It is pinned anyway, on a second and
# narrower criterion this block exists to state. The statement matters more
# than the pin: pinning packages nothing imports is what #29 had to undo when
# this file's dev counterpart carried streamlit's whole transitive tree, so an
# exception without a written boundary is just that mistake with better
# manners.
#
# The rule, in two clauses that have to be taken in order: pin an unimported
# transitive when (1) the pinned parent declares it with no upper bound, and
# (2) the parent is a thin wrapper over it — the child being where the
# behaviour lives. slowapi meets both. It declares `limits>=2.3` with no
# ceiling, and it supplies the decorator, the key function and the 429 handler
# (api/limiter.py, api/main.py:29-31) while limits supplies the window
# algorithm and the storage that every @limiter.limit(...) in
# api/routes.py:63,90,115 is evaluated by. Read out of the running image, the
# limiter behind those three decorators is limits' FixedWindowRateLimiter over
# limits' MemoryStorage — api/limiter.py passes neither, and slowapi resolves
# both out of limits by name: STRATEGIES["fixed-window"] and
# storage_from_string.
#
# Clause (1) is what most candidates fail, and failing it ends the question
# before judgment enters: starlette declares `anyio<5,>=3.6.2` and pydantic
# declares `pydantic-core==2.46.4`, so neither child can cross a major on a
# rebuild. A parent that later lifted its own cap would surface as a bump to
# that parent, in a diff. Only an unbounded declaration puts the child beyond
# review, which is the exposure this pin answers.
#
# Clause (2) is the judgment, and h11 under uvicorn is the case that shows
# where it stops. h11 clears clause (1) — `h11>=0.8`, no ceiling — is imported
# by nothing here, and is where HTTP/1.1 protocol behaviour actually lives, so
# it reaches the same question limits does. It is still not pinned, because
# uvicorn is not a thin wrapper over it: uvicorn is a server with config,
# lifespan, worker and signal handling, and h11 is one pluggable protocol
# implementation inside it. slowapi has no such surface of its own — strip
# limits out and nothing is left but the decorator. That is the line: is the
# parent's public behaviour the child's behaviour renamed, or does the parent
# merely run on the child. If it ever has to move, move it here in writing
# rather than package by package.
#
# The unbounded floor is not hypothetical: `limits>=2.3` has already been
# walked to 5.8.0, three majors, which is the starlette case above further down
# the same road. What those majors changed is the argument. 5.0.0 removed the
# fixed-window-elastic-expiry strategy outright, and slowapi selects a strategy
# by string key with `if strategy not in STRATEGIES: raise` — the set limits
# 5.8.0 offers is fixed-window, moving-window and sliding-window-counter, so a
# major that drops or renames the key this service defaults to takes rate
# limiting down at startup, from a rebuild with no manifest diff. 4.0.0 changed
# the reset value from get_window_stats from a truncated second to a float,
# which slowapi still annotates Tuple[int, int] and adds 1 to for its reset
# header; that one is inert here only because headers_enabled is False.
#
# Neither was noticed when it happened, because nothing here looks. No test
# under tests/ references the limiter, 429 or RateLimitExceeded. CI now builds
# the image and imports the app (the api-image job, added ahead of #52), which
# closes part of that gap: a limits major that breaks slowapi at import fails
# on the PR. It does not close the rest — the failure mode described above is a
# strategy key resolved at request time, and an import that succeeds says
# nothing about it, so this pin is still the whole of the defence there. A
# check that the limiter still limits would need a TestClient and so an httpx
# pin, which is a dependency decision of its own.
#
# Version derived from a --no-cache build of Dockerfile on 2026-07-28
# (linux/amd64, py3.11 — the Cloud Run runtime), read with importlib.metadata
# inside the image. Never re-derive on a developer machine. Dockerfile.pipeline
# resolves the same 5.8.0 through the `-r` inheritance, so this freezes a
# version both images already agree on rather than converging a split the way
# requests above did.
#
# The trade is starlette's in the same shape: a future slowapi that raises its
# limits floor past this pin fails the build instead of resolving quietly, and
# that failure lands in a diff. slowapi 0.1.10 — open as PR #55 in the
# fastapi-stack group — still declares `limits>=2.3`, so this pin survives that
# bump unchanged. limits is in that group too (.github/dependabot.yml), since a
# limits bump is not reviewable without the slowapi it runs under.
limits==5.8.0
