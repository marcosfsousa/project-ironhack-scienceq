# Dev, eval, and test dependencies — direct only, pinned. Transitives are
# whatever the resolver picks; do not pin them here by hand.
#
# This file is not dev-only despite the name: Dockerfile.pipeline installs it as
# the Cloud Run pipeline job's entire runtime. It therefore inherits the deploy
# manifest rather than restating it, so the pipeline image and the API image can
# never drift onto different versions of langchain, pinecone, or cohere the way
# they did before issue #29.
#
# That inheritance has a real cost, accepted deliberately: the pipeline image
# also pulls the API serving stack (fastapi, uvicorn[standard], slowapi,
# google-cloud-storage) which pipeline/ never imports. Tens of MB in a batch
# image is a cheaper failure than two production images silently disagreeing
# about a library version.
#
# The `-r` below is why Dockerfile.pipeline must COPY both files — pip resolves
# a nested requirements path relative to the referencing file's directory.
#
# Adding a dependency: placement follows what ships in an image, not which
# directory holds the importer. A pin here alone never reaches the API image —
# that is exactly how requests came to float there at a version the pipeline
# image did not ship (#42).
#
# Since #52 that question is finer than a directory. Dockerfile COPYs api/ and
# agent/ wholesale, but only four pipeline/ modules — live_ingest, and the
# cleaner, chunker and embedder it imports. So:
#
#   imported by api/, agent/, or one of those four   -> requirements.txt
#   imported only by the other six pipeline modules  -> here
#   imported only by eval/, tests/, scripts/         -> here
#
# The middle line is new and is the point of #52: a dependency that only the
# batch pipeline needs no longer has to land in the serving image. Nothing
# relies on you remembering which four — tests/test_declared_imports.py holds
# the split (SHIPPED_MODULES / UNSHIPPED_MODULES), checks it against
# Dockerfile's COPY on every run, and fails if a new pipeline/ module lands in
# neither list.
#
# Do not use .dockerignore as the test for that. It drops eval/ and tests/ from
# the build context, but scripts/ is not listed there at all — it ships nowhere
# only because neither Dockerfile's COPY allowlist names it. Both Dockerfiles
# enumerate what they copy, so the allowlist is what decides and .dockerignore
# narrows the context underneath it. The question to ask is whether a COPY names
# the path, not whether .dockerignore excludes it.
#
# The earlier rule read "only put it here when nothing under api/ or agent/
# imports it", which was written before anyone weighed the pipeline/ COPY. Under
# it requests was placed correctly and still diverged, so the criterion is what
# ships, not what imports.
#
# That rule decides which file a pin goes in. Whether an unimported package is
# pinned at all is a separate question, settled in #41 and written into the
# limits block at the bottom of requirements.txt: a transitive is pinned only
# when its parent declares it with no upper bound and is a thin wrapper over
# it. That is the narrow exception, not a licence to re-pin a transitive tree —
# which is what #29 removed from this file.
#
# The cost this file used to carry here — pipeline/ shipping in both images, so
# a future pipeline-only dependency landed in the serving image too, however
# heavy — is the one #52 removed, by narrowing the COPY rather than re-splitting
# these two files. Re-splitting is what produced the divergence in the first
# place, and the `-r` above is still what prevents it.
#
# What remains is smaller and worth stating so it is not mistaken for the old
# cost: requests is still pinned in requirements.txt even though no shipped
# module imports it any more, because it arrives in the API image transitively
# and carries an advisory floor. That is an exception with a written criterion,
# recorded in its block there — not a leftover to tidy back into this file.
-r requirements.txt

# ── Eval only ────────────────────────────────────────────────────────────────
# LLM-as-judge scoring in run_evals and the two sweep scripts.
#
# Bumped 2.26.0 → 2.49.0 on 2026-07-28 with the rest of the weekly batch (#40),
# and unlike every pin in requirements.txt this one is not covered by CI: the
# backend job installs requirements.txt alone, never this file, so a green run
# on an openai bump says nothing about it. Twenty-three minors is also too wide
# a span to have read release notes across. Accepted because the blast radius
# is eval/ — imported at run_evals.py:59 and both sweeps, none of which ships
# in either image or runs in CI. A break here costs a local eval run, not a
# deploy; the next `python -m eval.run_evals` is the real test.
openai==2.50.0

# ── Tests only ───────────────────────────────────────────────────────────────
# CI pins this same version explicitly (.github/workflows/ci.yml) — that pin is
# the one that decides what the suite runs against, since CI never installs this
# file. Bump the two together or CI silently keeps testing the old version:
# Dependabot only ever edits this line, so its green check on a pytest bump is
# measuring the version it just replaced.
pytest==9.1.1

# Note: .dockerignore excludes eval/ and tests/, so the pipeline image installs
# openai and pytest for code it never copies in. Pre-existing — the freeze this
# replaced did the same — and left alone rather than quietly narrowed, since
# splitting this file per-consumer is a separate change from issue #29.
